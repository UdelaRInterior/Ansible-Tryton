[Unit]
Description={{ item.value.systemd_description | default(item.value.tryton_systemd) }}

[Service]
ExecStart={{ item.value.systemd_start }}
ExecStop={{ item.value.systemd_stop }}
Restart=always
User={{ item.value.user }}
{% if item.value.systemd_enviroment is defined %}
{% for env in item.value.systemd_enviroment %}
Enviroment={{ env.name }}={{ env.value }}
{% endfor %}
{% endif %}
WorkingDirectory={{ item.value.systemd_work_dir }}

[Install]
WantedBy=multi-user.target
